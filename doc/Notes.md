 ```
        # Function1 Get_board_size: arg1:  one of (easy, medium, hard)
        # returns x and y dimensions (width and height of the board) (X,Y)

        # Function2 Get_number_of_bombs: arg1:  one of (easy, medium, hard)
        # returns number of bombs 

        # Function3 Build_board: arg1 x , arg2: y υ
        # returns list of lists(a two-dimensional list) of default tile objects

        # Function4 Get_position_of_bombs: input number of bombs N and number of tile objects X * Y
        # returns N random distinct numbers between 0 and X * Y exclusive(meaning -1) [0, X*Y)  [Random1,...,RandomN]
        
        # Fuction5 Get_coordinate_position_of_bombs : arg1: list of N distincts random numbers from Function4 , arg2: X, arg3: Y
        # Returns list of bomb coordinates [(b1_Y,b1_X),...,(bN_Y,bN_X]
        # Hint: use for(iterate) division (//) for Y ,  and % (module) for X to calculate b<i>_Y and b<i>_X 

        # Function6 Assign_bombs_to_tiles: input board and bomb indexes from Function5
        # returns board with bomb tiles assigned.

        # Function7 Assign_numbers_to_tiles: arg1: board from Function6 , arg2:X , arg3:Y 
        # returns board with tile numbers assigned.
 ```       
